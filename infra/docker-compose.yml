version: "3.9"

# -----------------------------------------------------------------------------
# DevTechs - docker-compose base
#
# Profiles
# --------
#   (none)        postgres + redis + (optional) dev tooling. This is the
#                 stack you want when developing — the apps and services
#                 themselves run natively on the host with `pnpm dev`,
#                 talking to containerized infra on localhost ports.
#   prod          The full production topology: nginx + every service +
#                 every Next.js app, all containerized, no host ports for
#                 the services (only nginx is exposed to the host).
#   dev-tools     Optional extras (adminer, mailhog, prisma studio).
#                 Add and gate behind this profile when you need them.
#
# Usage:
#   docker compose up -d                     # infra only (dev workflow)
#   docker compose --profile prod up -d      # full prod-shaped stack
#   docker compose --profile dev-tools up -d # tooling only
#
# All services share the `devtechs` network and read secrets from the
# root `.env` file (copy `.env.example` to `.env` and fill in real
# values).
# -----------------------------------------------------------------------------

x-service-defaults: &service-defaults
  restart: unless-stopped
  env_file:
    - ../.env
  networks:
    - devtechs
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_healthy
  profiles:
    - prod

services:
  # ---------------------------------------------------------------------------
  # Infra
  # ---------------------------------------------------------------------------
  postgres:
    image: postgres:16-alpine
    container_name: devtechs-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-devtechs}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devtechs}
      POSTGRES_DB: ${POSTGRES_DB:-devtechs}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - devtechs
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-devtechs} -d ${POSTGRES_DB:-devtechs}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    image: redis:7-alpine
    container_name: devtechs-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - devtechs
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  nginx:
    image: nginx:alpine
    container_name: devtechs-nginx
    restart: unless-stopped
    profiles:
      - prod
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/nginx/certs:ro
      # Static API docs (Redoc page + unified OpenAPI spec). Generated
      # by `pnpm docs:generate` at the repo root before each prod
      # deploy. Read-only mount.
      - ../docs:/usr/share/nginx/html/docs:ro
    networks:
      - devtechs
    depends_on:
      - auth-service
      - rh-service
      - finance-service
      - projects-service
      - devops-service
      - support-service
      - payments-service
      - notification-service
      - license-service
      - developer-service
      - web
      - store
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ---------------------------------------------------------------------------
  # NestJS microservices
  # ---------------------------------------------------------------------------
  auth-service:
    <<: *service-defaults
    container_name: devtechs-auth-service
    build:
      context: ..
      dockerfile: services/auth-service/Dockerfile
    environment:
      PORT: 3001
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
    expose:
      - "3001"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  rh-service:
    <<: *service-defaults
    container_name: devtechs-rh-service
    build:
      context: ..
      dockerfile: services/rh-service/Dockerfile
    environment:
      PORT: 3002
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
    expose:
      - "3002"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3002/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  finance-service:
    <<: *service-defaults
    container_name: devtechs-finance-service
    build:
      context: ..
      dockerfile: services/finance-service/Dockerfile
    environment:
      PORT: 3003
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
    expose:
      - "3003"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3003/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  projects-service:
    <<: *service-defaults
    container_name: devtechs-projects-service
    build:
      context: ..
      dockerfile: services/projects-service/Dockerfile
    environment:
      PORT: 3004
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
    expose:
      - "3004"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3004/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  devops-service:
    <<: *service-defaults
    container_name: devtechs-devops-service
    build:
      context: ..
      dockerfile: services/devops-service/Dockerfile
    environment:
      PORT: 3005
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
    expose:
      - "3005"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3005/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  support-service:
    <<: *service-defaults
    container_name: devtechs-support-service
    build:
      context: ..
      dockerfile: services/support-service/Dockerfile
    environment:
      PORT: 3006
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
    expose:
      - "3006"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3006/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  payments-service:
    <<: *service-defaults
    container_name: devtechs-payments-service
    build:
      context: ..
      dockerfile: services/payments-service/Dockerfile
    environment:
      PORT: 3007
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
    expose:
      - "3007"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3007/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  notification-service:
    <<: *service-defaults
    container_name: devtechs-notification-service
    build:
      context: ..
      dockerfile: services/notification-service/Dockerfile
    environment:
      PORT: 3008
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
    expose:
      - "3008"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3008/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  license-service:
    <<: *service-defaults
    container_name: devtechs-license-service
    build:
      context: ..
      dockerfile: services/license-service/Dockerfile
    environment:
      PORT: 3009
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      LICENSE_SIGNING_KEY: ${LICENSE_SIGNING_KEY}
    expose:
      - "3009"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3009/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  developer-service:
    <<: *service-defaults
    container_name: devtechs-developer-service
    build:
      context: ..
      dockerfile: services/developer-service/Dockerfile
    environment:
      PORT: 3010
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      AUTH_SERVICE_URL: ${AUTH_SERVICE_URL}
      AUTH_INTERNAL_SECRET: ${AUTH_INTERNAL_SECRET}
      JWT_SECRET: ${JWT_SECRET}
      DOCKER_SOCKET_PATH: /var/run/docker.sock
    expose:
      - "3010"
    # Mount the host Docker socket so dockerode can manage sibling
    # containers (start/stop/restart/logs). The developer-service
    # process must run as a user inside the host's `docker` group
    # for this to work without root — see Dockerfile.
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3010/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  # ---------------------------------------------------------------------------
  # Next.js frontends
  # ---------------------------------------------------------------------------
  web:
    restart: unless-stopped
    container_name: devtechs-web
    profiles:
      - prod
    build:
      context: ..
      dockerfile: apps/web/Dockerfile
    env_file:
      - ../.env
    environment:
      PORT: 4000
    expose:
      - "4000"
    networks:
      - devtechs
    depends_on:
      auth-service:
        condition: service_healthy

  # Module apps (rh / financeiro / projetos / devops / suporte / developer)
  # were retired and consolidated into `web` under /admin/* and /perfil/*.
  # Only `web` and the public planos/checkout SaaS `store` remain.
  store:
    restart: unless-stopped
    container_name: devtechs-store
    profiles:
      - prod
    build:
      context: ..
      dockerfile: apps/store/Dockerfile
    env_file:
      - ../.env
    environment:
      PORT: 4006
    expose:
      - "4006"
    networks:
      - devtechs
    depends_on:
      payments-service:
        condition: service_healthy

networks:
  devtechs:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
